import { createStore } from 'vuex'
import {authState} from './modules/auth.state'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authState
  }
})
